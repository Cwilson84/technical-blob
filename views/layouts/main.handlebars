<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <!-- Add Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <header class="my-4">
            <h1><a href="/" class="text-dark">Tech Blog</a></h1>
            <h2 class="subtitle">A tech resource and sharing community</h2>
            <nav class="my-3">
                {{#if loggedIn}}
                <a href="/dashboard" class="btn btn-primary">Create/Edit Posts</a>
                <button id="logout" class="btn btn-link">Logout</button>
                {{else}}
                <a href="/login" class="btn btn-primary">Login/Signup</a>
                <small class="text-muted">(you must be logged in to leave a comment)</small>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{body}}}
        </main>
        <footer class="mt-5">
            <p class="text-center">Thanks for stopping by!</p>
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="js/logout.js"></script>
    {{/if}}

    <!-- Add Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>